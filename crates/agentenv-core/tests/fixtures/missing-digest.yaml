version: 0.1.0
min_agentenv_version: 0.0.1
sandbox:
  driver: openshell
  image: ghcr.io/acme/agentenv-sandbox:latest
agent:
  driver: codex
context:
  driver: filesystem
  mount: ~/projects
inference:
  driver: passthrough
policy:
  tier: balanced
  presets: []
